<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" role="img" aria-label="Tsunami">
  <!--
    Tsunami mark. Colours are literal rather than CSS custom properties on
    purpose: this file loads through an img element, which isolates it from the
    page stylesheet, so tokens would not resolve. Keep in sync with the accent
    tokens in src/app.css.

    Do not write a token name literally in this comment. XML forbids a double
    hyphen inside a comment, so doing that makes the whole file invalid XML and
    it renders as a broken image with nothing logged to the console.
  -->
  <g fill="none" stroke="#1FA8FF" stroke-width="8.5" stroke-linecap="round" stroke-linejoin="round">
    <path d="M12 58C12 30 34 12 58 12c18 0 30 12 30 28 0 12-9 21-21 21-9 0-16-7-16-16"/>
    <g opacity="0.55">
      <path d="M6 77q11-10 22 0t22 0 22 0 22 0"/>
      <path d="M6 91q11-10 22 0t22 0 22 0 22 0"/>
    </g>
  </g>
</svg>
